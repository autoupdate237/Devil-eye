# Devil's Eye core is stdlib-only by design (it must always start).
# Optional extras widen telemetry coverage on Windows:
#   pip install .[win]    -> psutil, pywin32
#   pip install .[dev]    -> pytest
#   pip install .[build]  -> pyinstaller (single-EXE packaging)
